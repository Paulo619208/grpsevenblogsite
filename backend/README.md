# Backend (Express API)

## Setup
1. Install dependencies:
   - `npm install`
2. Copy `.env.example` to `.env` if you need custom settings.
3. Run server:
   - `npm run dev` (development)
   - `npm start` (production style)

## Endpoints
- `GET /health`
- `GET /api/articles/payment-methods`
